services:
  backend:
    volumes:
      - ./backend/src:/app/src
    environment:
      DEBUG: "1"
      FLASK_ENV: development
    command: ["python", "-m", "flask", "--app", "src.app", "run", "--host", "0.0.0.0", "--port", "5000", "--reload"]

  db:
    ports:
      - "5432:5432"